{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/snackticket-server-api-contract-test/src",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run graphql-event"
        ],
        "cwd": "packages/snackticket-server-api-contract-test"
      }
    },
    "graphql-event": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "npx ts-node prisma-seeds/reset.ts",
          "npx nx serve-test snackticket-server &",
          "sleep 5 && npx ts-node prisma-seeds/event.seeding.ts",
          "npx ts-node event.spec.ts",
          "npx ts-node prisma-seeds/reset.ts"
        ],
        "cwd": "packages/snackticket-server-api-contract-test/src/specs/graphql",
        "parallel": false,
        "envFile": "packages/snackticket-server/.env.test"
      }
    }
  },
  "tags": []
}
